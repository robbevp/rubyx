[project]
name = "exercises"
version = "0.1.0"
description = "Persistent context exercises with ML model loading"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0",
    "transformers>=4.40",
]

[tool.uv.workspace]
members = [
    "examples/python",
]
